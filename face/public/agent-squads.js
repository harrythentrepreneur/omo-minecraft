// No-op stub for the omo-mc face. Real omo orchestrates choreographed
// sprite squads when a research agent runs; face/ has no agent dispatch
// surface, so this is a no-op installer.
export function installSquadLayer(_spriteLayer) {}
